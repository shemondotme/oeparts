BULK UPDATE APPLIED — OeParts

{{ $adminName }} applied "{{ $actionLabel }}" to {{ number_format($affectedCount) }} products —
above the automatic large-batch alert threshold.

Log in to the admin panel > Catalog > Bulk Update Log to review exactly what changed,
or revert it from that page if this was unintended.
